<div class="container">
  <div class="menu">
    {{#link-to "index"}}
      <h1>
        <em>Flower Shop</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to "about" class="menu-about"}}
        Om oss
      {{/link-to}}<br>
      {{#link-to "contact" class="menu-contact"}}
        Kontakta oss
      {{/link-to}}
    </div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
</div>